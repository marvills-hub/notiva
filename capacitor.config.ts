import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.marvills.notiva',
  appName: 'Notiva',
  webDir: 'dist/notiva/browser',
};
export default config;
